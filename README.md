# Daniel Kindl

I tell computers what to do and they listen. Sometimes.

```csharp
public record Whoami
{
    public string[] Principles => new[] { "TDD", "SOLID", "Clean Architecture" };
    public string[] Languages => new[] { "C#", ".NET", "TypeScript", "Astro", "Svelte", "Python" };
    public string Method => "I design the system. AI does a lot of the typing. I own the result.";
}
```

Currently building [TangoByteLens](https://github.com/daniel-kindl/TangoByteLens) — a binary file inspector that runs entirely in your browser.
